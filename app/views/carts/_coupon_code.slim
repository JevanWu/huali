.discount
    p
      | 使用优惠码

    br
    = simple_form_for @cart, url: carts_coupon_path, method: :post do |f|
        = f.association :coupon_code, as: :string, label: false
        = f.submit "确定", class: "btn-discount"
