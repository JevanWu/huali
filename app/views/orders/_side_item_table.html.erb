<table class='side-table table'>
  <thead>
    <tr>
      <th colspan='3'><%= t('views.order.basket') %></th>
    </tr>
  </thead>
  <tbody>
    <%= render :partial => 'side_item_table_each', :collection => items, :as => :item %>
  </tbody>
  <tfoot>
    <tr>
      <td colspan='2'><%= t('views.order.in_total') %></td>
      <td><%= count_cart(items) %></td>
    </tr>
  </tfoot>
</table>
