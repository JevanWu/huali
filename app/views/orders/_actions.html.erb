<div class="order-actions">
 <%= link_to t('views.order.pay_now'), checkout_order_path(@order), class: 'button' %>
 <%= link_to t('views.order.cancel_now'), checkout_order_path(@order), class: 'button-alt' %>
</div>
