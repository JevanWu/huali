.eachorder
  .product
    = image_tag(item.product.assets.first.image.url)
  .name
    p
      = item.product.name_zh
    p.en
      = item.product.name_en
  .sprice
    p
      = "¥ #{item.product.price}"
  .qu
    p 
      = item.quantity
  .dis
    p 
      - if item.order.discounted?
        = item.order.discounted_money
  .total
    p 
      = "¥ #{item.quantity * item.product.price}"
