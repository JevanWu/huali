<% if current_user %>
  <script>
    $(function () {
      analytics.identify('<%= current_user.id %>', {
        name: '<%= current_user.name %>',
        email: '<%= current_user.email %>'
      });
    });
  </script>
<% end %>
