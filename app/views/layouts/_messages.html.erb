<%= content_tag :div, 'For English Service: 400-001-6936', class: 'flash', id: "flash_ad"  %>

<% flash.each do |name, msg| %>
  <%= content_tag :div, msg, id: "flash_#{name}", class: 'flash' if msg.is_a?(String) %>
<% end %>
