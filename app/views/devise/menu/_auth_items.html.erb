<div id='auth-item'>
<% if user_signed_in? %>
  <%= link_to current_user.username, orders_path %>
  <%= link_to '退出', destroy_user_session_path, method: :delete, class: 'subtle' %>
<% else %>
  <%= link_to '注册', new_user_registration_path %>
  或
  <%= link_to '登录', new_user_session_path  %>  
<% end %>
</div>
