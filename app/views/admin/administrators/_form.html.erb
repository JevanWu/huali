<%= semantic_form_for [:admin, @administrator] do |f| %>
  <%= f.inputs do %>
    <%= f.input :email %>
    <%= f.input :password %>
    <%= f.input :role, :collection => %w(super admin supplier) %>
  <% end %>
  <%= f.actions %>
<% end %>
