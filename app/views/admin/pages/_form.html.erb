<%= semantic_form_for @page do |f| %>
  <%= f.inputs do %>
    <%= f.input :name %>
    <%= f.input :permalink %>
    <%= f.input :content, :hint => "Use Markdown to write the content"  %>
  <% end %>

  <%= f.actions %>
<% end %>
