<%= semantic_form_for [:admin, @sync_order] do |f| %>
  <%= f.inputs do %>
    <%= f.input :administrator_id, as: :hidden, input_html: { value: current_administrator.try(:id) } %>
    <%= f.input :kind %>
    <%= f.input :merchant_order_no %>
  <% end %>
  <%= f.submit %>
<% end %>
