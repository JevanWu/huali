<%= semantic_form_for [:admin, @story] do |f| %>
  <%= f.inputs do %>
    <%= f.input :name %>
    <%= f.input :description %>
    <%= f.input :picture, as: :file %>
    <%= f.input :available %>
  <% end %>
  <%= f.submit %>
<% end %>

